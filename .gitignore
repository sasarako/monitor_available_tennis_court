# Local config — has secrets. GHA renders this from config.template.json + secrets at runtime.
config.json

# Runtime artifacts
*.log
last_response.json
launchd.stdout.log
launchd.stderr.log

# Probe script with real tokens pasted in — never commit
probe_talent_refresh_real.py

# macOS / editor cruft
.DS_Store
__pycache__/
*.pyc
.venv/
.env
